spring:
  application.name: utils-monitoring-application
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      username: user
      password: none
  main:
    allow-bean-definition-overriding: true
